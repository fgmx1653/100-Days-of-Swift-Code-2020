import SwiftUI


struct ContentView: View {
    var body: some View {
        Text("Starting over with this project, I need to fix the package dependencies.")
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
